<div class="velo-loader loader">
  <div class="square">
    <div></div>
    <div></div>
  </div>
</div>

<style scoped>
  .loader {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .loader p {
    color: #0a8737;
    font-weight: 400;
  }

  .square {
    height: 100%;
    width: 100%;
    position: relative;
    perspective: 500%;
  }

  .square div {
    position: absolute;
    top: 0;
    height: 24px;
    width: 24px;
    background: #00b140;
    animation: flip 2s linear infinite;
    transform-origin: right bottom;
  }

  .square :nth-child(2) {
    animation-delay: 1s;
    opacity: 80%;
  }

  @keyframes flip {
    0% {
      transform: rotateX(0) rotateY(0);
    }
    25% {
      transform: rotateX(0) rotateY(180deg);
    }
    50% {
      transform: rotateX(180deg) rotateY(180deg);
    }
    75% {
      transform: rotateX(180deg) rotateY(0);
    }
    100% {
      transform: rotateX(0) rotateY(0);
    }
  }
</style>